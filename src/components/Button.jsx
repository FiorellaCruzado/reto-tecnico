import React from 'react';

const Button = () => {
    return (
        <button
        //   disabled={disabled}
          className={`${styles.Button} ${className} ${grey && styles.grey}`}
        //   onClick={onClick}
        >
        </button>
    );
};

export default Button;